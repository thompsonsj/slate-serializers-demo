/**
 * `Element` class from domhandler (used to build nodes in markTransforms / elementTransforms).
 * For TypeScript types that appear in config APIs, prefer:
 * `import type { Element, ChildNode, Text } from '@slate-serializers/html'`
 * (or `slate-serializers`) — see https://github.com/thompsonsj/slate-serializers/pull/215
 */
export { Element } from 'domhandler'
